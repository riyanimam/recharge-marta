/**
 * Stub for Nuxt's #imports auto-import so composable tests can import
 * `reactive`, `computed`, `ref`, etc. without a full Nuxt runtime.
 */
export { computed, onMounted, onUnmounted, reactive, ref } from "vue";
